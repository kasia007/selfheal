# SPEC · selfheal (스스로 고치는 코드 에이전트)

> **깨진 테스트가 있는 디렉터리 하나를 주면, 무엇을 고쳐야 하는지 스스로 찾아 고치고,
> 같은 실수가 반복되는지를 기억해 알려 주는 에이전트입니다.**

아래 1~5절은 제출 규약(§4-3)이 요구하는 템플릿 순서 그대로입니다. 그 뒤 부록에 같은
내용을 더 깊이 다루는 절이 이어집니다 — 필요할 때만 펼쳐 보면 됩니다.

---

## 1. 사용자·문제·가치

### 사용자
- 로컬에서 테스트가 깨진 채로 붙잡혀 있는 개발자
- 여러 언어의 습작/샘플 프로젝트를 오가는 교육 과정 수강생
- CI 에서 반복 실패하는 사소한 버그를 매번 손으로 고치는 팀

### 문제
경계 검사 누락, None/nil 역참조, 부재 키 접근 같은 **정형화된 실수**는
고치는 방법이 거의 정해져 있는데도 매번 사람이 붙어야 합니다. 더 나쁜 것은
**같은 팀이 같은 실수를 반복하는데 그 사실을 아무도 모른다**는 점입니다.
개인은 자기가 세 번째로 같은 버그를 만들고 있다는 것을 인지하지 못합니다.

### 가치
selfheal 이 내놓는 것은 두 겹입니다.

- **1차 — 수정**: 디렉터리 하나만 주면 대상 파일까지 스스로 찾아 고치고,
  테스트로 검증한 diff 를 냅니다.
- **2차 — 재발 인식**: "이 패턴은 4회째입니다" 를 알려 줍니다.
  자동 수정보다 이쪽이 실제로는 더 쓸모가 있습니다.
  개인의 실수를 팀의 **관측 가능한 지표**로 바꾸기 때문입니다.

> 이 서비스가 답하는 질문은 "이 에러를 어떻게 없애지?" 가 아니라
> **"우리는 무슨 실수를 반복하고 있는가?"** 입니다.

---

## 2. 서비스 확장 관점

**CI 파이프라인에 상시 연동하는 사내 도구로 확장 가능합니다.** 지금도 `install-hooks.sh`
로 `pre-push`·`post-commit` 훅을 걸 수 있으므로, 개인 로컬 실습을 넘어 **팀 저장소에
바로 붙일 수 있는 형태**가 이미 준비돼 있습니다.

- **비용 절감 관점** — 릴리스 전 반복되는 정형화된 버그를 사람이 리뷰하기 전에 자동으로
  감지·제안함으로써, 코드 리뷰어가 "또 그 실수" 를 매번 지적하는 시간을 줄입니다.
- **조직 학습 관점** — `--stats` 가 팀 전체(같은 selfheal 인스턴스를 공유하는 저장소들)의
  반복 실수를 발생 횟수 내림차순으로 보여 주므로, 온보딩 자료나 코드 컨벤션 개정의
  근거 데이터가 됩니다. 이게 "돈을 버는" 기능이 아니라 "같은 실수를 두 번 안 하게
  만드는" 조직 학습 도구로서의 확장 지점입니다.
- **규모** — 저장소당 selfheal 인스턴스 하나, 언어별로 메모리가 격리되므로 여러 팀이
  각자의 저장소에 독립적으로 붙여도 서로 오염되지 않습니다(부록 C 참고).
- **차별점** — 원본 참고 구현(부록 A)과 달리 검색 결과를 실제로 수정 프롬프트에 주입해
  재시도 횟수 감소를 수치로 증명하므로, "AI 가 그냥 대충 고쳐 준다" 가 아니라 학습
  효과를 리포트로 보여줄 수 있는 도구입니다.

---

## 3. 사용 예상 도구·데이터

### 도구 (그래프 노드 9개)

| 노드 | 하는 일 | LLM 사용 |
|---|---|---|
| `test_execution_node` | 서브프로세스로 테스트 실행, 통과/빌드실패/테스트실패 판정 | ✗ |
| `locate_node` | 스택트레이스에서 수정 대상 추론 (규칙 → LLM 폴백) | △ |
| `bug_report_node` | 무엇이 왜 터졌는지 정리 | ✓ |
| `memory_search_node` | 저장 포맷과 같은 템플릿으로 압축 후 벡터 검색 | ✓ |
| `memory_filter_node` | 거리 임계값으로 "같은 패턴" 선별 | ✗ |
| `memory_modification_node` | 기존 패턴에 누적 병합 + **발생 횟수 +1** | ✓ |
| `memory_generation_node` | 새 패턴 저장 (1회 발생) | ✗ |
| `code_update_node` | **과거 사례를 주입해** 수정본 생성 | ✓ |
| `code_patching_node` | **샌드박스 사본에** 파일 쓰기 | ✗ |

### 데이터

| 소스 | 내용 |
|---|---|
| `data/samples/py-index` | warmup — 경계 검사 누락 (Python) |
| `data/samples/py-dict` | eval — 부재 키 접근 (Python, 같은 계열 다른 형태) |
| `data/samples/js-head` | eval — undefined 역참조 (JS, 언어 전이 실험) |
| `chroma_db/` | 누적 버그 패턴. `occurrences`·`languages`·`first_seen`·`last_seen` metadata |
| `evaluation/eval_set.json` | 인-아웃 세트 (실행기용 내부 사양) |
| `evaluation/test_queries.csv` | 위와 같은 세트의 제출 규약 스키마 (§4-4) |

### 스택 (AGENTS.md 고정 — 임의 변경 금지)
- LLM: `us.anthropic.claude-sonnet-4-5-20250929-v1:0` (Bedrock)
- 임베딩: `amazon.titan-embed-text-v2:0`
- 벡터DB: Chroma `PersistentClient`
- 오케스트레이션: LangGraph `StateGraph`

---

## 4. 서비스 정책 (가드레일 요약)

어떤 경우에도 지켜야 할 규칙 5가지입니다. 세부 11건(G1~G11)은 **부록 D**에서 검증
방법과 함께 다룹니다.

1. **테스트 파일·설정 파일·의존성 디렉터리는 절대 수정하지 않는다** (G1·G3·G4)
2. **사용자 승인(`--apply`) 없이는 원본에 쓰지 않는다** — 모든 수정은 샌드박스 사본에서
   검증한 뒤 diff 로만 제시한다 (G6·G8)
3. **재시도·수정 대상 폭주에 상한을 둔다** — 재시도 3회, 고치라고 하지 않은 심볼은
   지우지 않는다 (G5·G9)
4. **모델을 쓸 수 없거나 전제가 안 맞으면 추측하지 않고 즉시 중단한다** (G2·G7, exit 3·5)
5. **비밀값은 나가는 곳에서 지우고, 신뢰할 수 없는 입력은 데이터로만 다룬다** (G10·G11)

---

## 5. 성공 기준

### 반드시 만족 (하나라도 실패하면 미달)
1. **가드레일 G1~G11 위반 0건** — `tests/test_guardrails.py` 전부 통과
2. **인-아웃 세트 통과율 ≥ 0.8** — `evaluation/eval_set.json` 기준
3. **자격증명 없이 배선 검증 가능** — `tests/test_wiring.py` 전부 통과
4. exit code 0/1/2/3/4/5 가 정의대로 동작
5. **기본 실행이 원본을 수정하지 않음** — 승인 흐름이 구조로 보장됨

### 목표
6. warmup 이후 메모리 **주입 건수가 0에서 증가** — 2026-09-03 실측: py-dict +2건, js-head +1건,
   **달성.** 단, "평균 시도 횟수 감소"는 현재 샘플이 전부 1회 시도로 풀리는 난이도라 이번
   실측으로는 증명되지 않았다(부록 C 참고) — 시도 횟수로도 보려면 더 어려운 샘플이 필요하다
7. 최소 2개 언어에서 동일 에이전트가 동작 (Python, JavaScript) — 2026-09-03 실측: 둘 다
   시도 1회로 수정 성공, **달성**
8. `--stats` 가 재발 패턴을 발생 횟수 내림차순으로 보고

### 명시적 비목표
- 설계 결함이나 성능 문제 수정 — 정형화된 실수 계열만 다룹니다.
- 테스트가 없는 프로젝트 — 판정 기준이 없으므로 exit 3 으로 거절합니다.
- 여러 파일에 걸친 수정 — 현재는 실패 지점 파일 하나만 고칩니다.

---
---

# 부록 — 필수 5절의 상세 근거

여기부터는 제출 템플릿에 없는 추가 절입니다. 위 5절이 "무엇을 왜" 를 답한다면,
여기는 "어떻게" 를 코드 경로까지 근거로 답합니다.

## 부록 A. 이 서비스의 설계 축 (가장 중요한 규칙)

**"고쳤다"의 판정 권한을 LLM 에게 주지 않습니다. 오직 테스트만이 판정합니다.**

이 한 줄이 나머지 설계를 전부 결정합니다.

원본 참고 구현(NirDiamant/GenAI_Agents 의 `self_healing_code.ipynb`)은 LLM 에게
"에러를 우아하게 처리해서 에러 메시지를 반환하라"고 시키고, 예외가 안 나면 성공으로
판정했습니다. 그 결과 `divide(10, 0)` → `"Error: division by zero"` 문자열 반환이
정답 취급됩니다. **이것은 버그를 고친 것이 아니라 삼킨 것입니다.**

그래서 이 서비스는 판정 기준을 밖으로 빼냈습니다.

| | 판정 주체 | 결과 |
|---|---|---|
| 원본 | LLM 자기 선언 | 검증 불가, 에러 은폐 |
| **selfheal** | **대상 프로젝트의 테스트** | 통과/실패가 객관적, CI 연동 가능 |

부수 효과로 두 가지가 따라옵니다.
1. 테스트가 언어별 명령이므로 **언어 중립**이 됩니다. (`exec` 를 버릴 수 있음)
2. LLM 이 만든 코드가 우리 프로세스에서 실행되지 않으므로 **샌드박싱**이 해결됩니다.

### 두 번째 축 — 제안 → 검수 → 적용

**에이전트는 사용자 승인 없이 원본 코드를 고치지 않습니다.**

수정과 검증은 작업 디렉터리의 **임시 사본(샌드박스)** 에서 이뤄집니다.
그래서 사용자가 받는 diff 는 이미 "이 패치는 테스트를 통과한다" 가 검증된 것이고,
원본은 그 시점까지 한 번도 열리지 않습니다.

```
기본            run.sh <dir>            → 검증된 수정안 + diff, 원본 불변 (exit 4)
사용자 검수 후   run.sh <dir> --apply    → 그때 원본에 적용 (exit 0)
```

자동 적용을 기본값으로 두고 `--dry-run` 을 옵션으로 주는 것은 방향이 반대입니다.
**되돌리기 어려운 쪽이 기본값이 되어서는 안 됩니다.**

## 부록 B. 인·아웃 세트 상세

### 입력 (사용자가 주는 것)

**디렉터리 경로 하나**입니다. 그 외는 전부 선택입니다.

```bash
./run.sh ./data/samples/py-index
```

| 항목 | 필수 | 없으면 |
|---|---|---|
| `workdir` | **필수** | 실행 불가 (exit 3) |
| 언어 | 선택 | 마커 파일 → 확장자 최빈값으로 추론 |
| 테스트 명령 | 선택 | 어댑터 기본값 / `package.json` 의 `scripts.test` |
| **고칠 파일** | **받지 않음** | 테스트 실패 스택트레이스에서 추론 |

**의도적으로 받지 않는 입력**이 두 가지 있습니다.
- **수정 방법 힌트** — 받으면 에이전트가 아니라 치환기가 됩니다.
- **테스트 수정 권한** — 열어 주면 LLM 이 테스트를 고쳐서 통과시킵니다. (가드레일 G1)

### 출력

| 층 | 내용 | 용도 |
|---|---|---|
| 화면 | 진행 로그 + **diff** | 사람이 리뷰 |
| `.heal/report.json` | 상태·시도 횟수·메모리 히트 | 기계가 집계 |
| `.heal/patch.diff`, `trace.log` | 적용 diff, 프롬프트 전문 | 재현·디버깅 |
| exit code | 0/1/2/3/4/5 | CI 연동 |

exit code 를 여섯 갈래로 나눈 것이 측정 가능성의 핵심입니다.

| 코드 | 의미 | 왜 분리하는가 |
|---|---|---|
| 0 | 고침 (`--apply` 로 실제 적용) | |
| 1 | 못 고침 (재시도 소진) | |
| 2 | **고칠 게 없음** (테스트 전부 통과) | 1과 섞으면 "멀쩡한데 실패"로 오독됨 |
| 3 | 전제 실패 (툴체인·테스트 없음, 언어 미지원) | 에이전트 성능 문제가 아님 |
| **4** | **검증된 수정안 준비됨, 승인 대기** (기본 동작) | 0과 섞으면 "이미 고쳐졌다"고 오독됨. CI 는 4를 "리뷰 필요"로 다룸 |
| **5** | **모델을 쓸 수 없었음** (토큰 한도·인증·네트워크) — 진단과 참고 사례만 제시 | 1과 섞으면 "코드를 못 고쳤다"로 오독됨. 코드 결함이 아니라 인프라 제약이라 재실행하면 고쳐질 수 있고, 훅에서 푸시를 막아서도 안 된다 (부록 D "모델을 쓸 수 없을 때") |

### 측정 가능성

모든 실행이 `report.json` 을 남기므로 다음 값이 자동으로 모입니다.

| 지표 | 정의 | 목표 |
|---|---|---|
| **수정 성공률** | exit 0 / (exit 0 + exit 1) | ≥ 0.8 |
| **평균 시도 횟수** | 성공 케이스의 `attempts` 평균 | warmup 이후 **감소** |
| **메모리 주입률** | `injected` 가 true 인 케이스 비율 | warmup 이후 ≥ 0.8 |
| **가드레일 위반** | G1~G11 위반 건수 | **0건 (하나라도 위반 시 실패)** |
| RAGAS | context precision/recall, faithfulness, relevancy | 보조 지표 |

## 부록 C. 경험 축적 효과 (이 서비스의 핵심 실험)

이 프로젝트에서 "개선"은 프롬프트를 손보는 것이 아니라
**에이전트가 과거 경험을 쌓아 스스로 나아지는 것**입니다.

메모리는 **항상 켜져 있습니다.** 예전에는 `--no-memory` 로 OFF 기준선을 만들어 ON/OFF 를
대조했지만, 옵션과 함께 그 대조 구조도 없앴습니다. 대신 **무엇이 주입되었는지**를 근거로
삼습니다 — `report.json` 의 `memory_hits[].injected` 와 `attempts` 입니다.

측정 설계에서 두 가지를 반드시 지킵니다.

1. **같은 버그를 다시 돌리지 않습니다.** 그건 캐시지 학습이 아닙니다.
   `warmup` 케이스로 패턴을 쌓고, **계열만 같고 형태가 다른** `eval` 케이스를 채점합니다.
2. **처음 실행에는 주입이 일어나지 않습니다.** 메모리가 비어 있기 때문입니다.
   효과는 warmup 이후부터만 유효합니다.

**실측 (2026-09-03, Bedrock Sonnet 4.5 + Titan v2, `evaluation/run_ragas.py` 온라인 실행,
`--merge-threshold 0.55`):**

| | 주입 0건 (warmup 전) | 주입 발생 (warmup 후) | 개선폭 |
|---|---|---|---|
| py-dict 평균 시도 | 1.00회 | 1.00회 | **0회 (측정 안 됨)** |
| js-head 평균 시도 | 1.00회 | 1.00회 | **0회 (측정 안 됨)** |
| py-dict 주입 건수 | 0건 | 2건 | +2건 |
| js-head 주입 건수 | 0건 | 1건 | +1건 |
| 수정 성공률 | 3/3 (100%) | 3/3 (100%) | 0%p (이미 상한) |

> **정직한 한계** — 이번 샘플(`py-index`·`py-dict`·`js-head`)은 전부 **1회 시도로 풀리는
> 난이도**라서, 메모리 주입 여부와 무관하게 `attempts` 가 항상 1입니다. 그래서 "평균 시도
> 횟수 감소"(5절 목표 6)는 **지금 샘플로는 증명되지 않습니다.** 대신 **주입 건수(0→2, 0→1)**
> 가 메모리가 실제로 검색되어 프롬프트에 실렸다는 유일한 실측 증거입니다. 시도 횟수로
> 개선을 보려면 한 번에 안 풀리는 난이도의 샘플이 추가로 필요합니다 — 현재는 5절 비목표
> 범위를 지키기 위해 샘플을 일부러 단순하게 유지하고 있어 이 트레이드오프를 받아들입니다.

> **주의** — 주입이 0건인데 시도가 적다면 메모리와 무관하게 쉬운 버그였다는 뜻입니다.
> 두 값을 반드시 함께 봐야 합니다. 위 표가 정확히 그 사례입니다.

> **주의** — 참고한 원본 노트북은 이 실험이 애초에 불가능합니다.
> 메모리를 검색해 놓고 **수정 프롬프트에 넣지 않기** 때문에 ON/OFF 결과가 같습니다.
> 검색 결과를 실제로 주입하는 것이 이 구현의 핵심 기여입니다.

## 부록 D. 가드레일 상세 (11건)

에이전트가 **절대 하지 않아야 하는 것**들입니다.
전부 코드로 강제하고, `tests/test_guardrails.py` 로 실제 준수를 검증합니다.
**하나라도 위반하면 그 실행은 실패로 간주합니다.**

| ID | 가드레일 | 왜 필요한가 | 강제 위치 |
|---|---|---|---|
| **G1** | **테스트 파일을 수정하지 않는다** | 열어 주면 LLM 이 테스트를 고쳐서 통과시킨다. 가장 자주 터지는 실패 모드 | `adapters.is_source_file` |
| **G2** | 작업 디렉터리 밖을 수정하지 않는다 | 스택트레이스에 표준 라이브러리 프레임이 섞여 들어온다 | `adapters.locate_target` |
| **G3** | 의존성·빌드 산출물을 수정하지 않는다 (`node_modules`, `vendor`, `site-packages`, `dist`) | 우리 코드가 아니다 | `EXCLUDED_DIRS` |
| **G4** | 설정·매니페스트 파일을 수정하지 않는다 (`pyproject.toml`, `package.json`, `go.mod`) | 테스트 설정을 무력화해 통과시키는 우회로 | 확장자 화이트리스트 |
| **G5** | **재시도 상한을 넘기지 않는다** (기본 3회) | 원본에는 제동장치가 없어 무한 루프가 가능 | `error_router` |
| **G6** | **사용자 승인(`--apply`) 없이 원본에 쓰지 않는다** | 되돌리기 어려운 동작이 기본값이면 안 된다. 사람이 diff 를 검수할 틈이 있어야 한다. 실패했을 때 "반쯤 고친 코드"가 남지 않는 것도 여기서 자동으로 따라온다 | `src/agent.py` 샌드박스 + `--apply` |
| **G7** | 전제 불충족이면 **LLM 을 호출하지 않고** 즉시 중단한다 | 툴체인이 없으면 테스트는 무조건 실패하고, 멀쩡한 코드를 고치려 들며 토큰만 태운다 | `preflight`, `has_tests` |
| **G8** | 수정은 언제나 샌드박스 사본에서 검증한 뒤에만 제시한다 | 검증되지 않은 패치를 사람에게 들이밀지 않는다. `--dry-run` 은 기본 동작의 별칭 | `src/agent.py` 샌드박스 |
| **G9** | **고치라고 하지 않은 심볼을 지우거나 시그니처를 바꾸지 않는다** | 수정안은 파일 **전체**를 재생성한다. 무관한 함수가 조용히 사라져도 테스트가 그 부분을 덮지 않으면 그대로 통과한다 | `tools.extract_symbols` + `code_update_node` |
| **G10** | **비밀값은 남는 곳·나가는 곳에서 지운다** | 소스에 하드코딩된 키·토큰이 `trace.log`·화면·LangSmith 로 새어 나가면 안 된다. 단, **모델에 가는 프롬프트는 원본을 유지**한다 — 마스킹하면 파일 전체 재생성 특성상 `***` 가 사용자 코드에 그대로 쓰일 위험이 있다 | `src/guardrails.py mask_secrets` + `Console._mask` + LangSmith `hide_inputs` |
| **G11** | **신뢰할 수 없는 입력(소스·테스트 출력)을 데이터로만 다룬다** | 저장소가 프롬프트 인젝션을 심어 둘 수 있다. 탐지되어도 **차단하지 않고 기록만** 한다 — "무시하고" 같은 문구는 정상 주석에도 나와 오탐에 취약하다. 진짜 방어는 G1·G6·G9 다 | `src/guardrails.py wrap_untrusted`·`detect_injection` |

### 구조화 출력 (체크리스트 #1)

LLM 호출 다섯 곳 중 네 곳을 `ChatPromptTemplate | llm | PydanticOutputParser` 체인으로
바꿨습니다(`src/schemas.py` — `TargetChoice` · `BugReport` · `BugPattern`).

`with_structured_output()` 을 쓰지 않은 이유는 **오프라인 검증이 불가능**하기 때문입니다.
LangChain 공식 가짜 채팅 모델도 그 메서드를 구현하지 않아(`NotImplementedError`),
tool-use 를 상상으로 흉내낸 가짜를 만들어야 합니다. 이 프로젝트에서 가짜 모델은 편의가
아니라 가드레일 검증의 핵심 도구(일부러 나쁘게 구는 모델로 G1·G5·G9 를 시험)이므로,
흉내가 실제 동작과 어긋나면 검증 전체가 거짓 안심이 됩니다.

저장 형식은 문자열을 유지합니다 — Chroma 가 임베딩하는 대상이 문자열이어야 하므로,
`BugPattern` 은 파싱·검증 계층으로만 쓰고 저장 직전에 `render()` 로 기존 형식
(`# 패턴명 ## 증상 ### 원인과 해결`)으로 되돌립니다. 이미 쌓인 `chroma_db` 와 호환됩니다.

### 검색 파이프라인 (체크리스트 #3)

`MemoryStore.search` 는 세 층입니다 — 쿼리 확장 → 하이브리드(벡터+BM25, RRF 융합) →
도메인 리랭킹(`occurrences`·언어 일치·최근성).

설계에서 지킨 원칙이 하나 있습니다. **17단계에서 `embed_query` 누락으로 검색이 한 번도
동작하지 않았다는 것을 발견했으므로, 이 프로젝트의 벡터 검색은 아직 실제 데이터로 평가된
적이 없습니다.** 그 위에 장치를 얹으면 무엇이 기여하는지 모르는 상태가 됩니다 —
검증되지 않은 `--merge-threshold 0.3` 에서 이미 겪은 문제입니다.

그래서 **기여도를 측정 가능하게** 만들었습니다. 각 hit 에 `vector_rank`·`bm25_rank`·
`final_rank`·`boost` 를 남겨, 한도가 회복된 뒤 온라인 실행 한 번으로 "하이브리드가 실제로
도움이 됐는가" 를 숫자로 판단할 수 있습니다.

RRF 를 택한 이유는 **가중치라는 상수를 만들지 않기 위해서**입니다. 코사인 거리와 BM25
점수는 비교 불가능한 척도이고, BM25 점수 범위는 코퍼스에 따라 달라져 가중치가 데이터마다
어긋납니다. RRF 는 순위만 쓰므로 그 문제가 사라집니다.

### 모델을 쓸 수 없을 때 (축약 경로, exit 5)

Bedrock 일일 토큰 한도에 걸리면 예전에는 **raw traceback 으로 죽고 `.heal/` 산출물도 남지
않았습니다.** 관측을 강조하면서 정작 실패하면 근거가 없는 모순이라 축약 경로를 만들었습니다.

핵심은 **메모리 검색이 살아 있을 수 있다**는 점입니다 — 검색은 `ChatBedrock`(Sonnet)이
아니라 Titan 임베딩을 쓰므로 별도 한도를 갖습니다. 그래서 고치지는 못하더라도 "과거에 이
계열 버그를 어떻게 고쳤는지" 는 찾아 단서로 남깁니다.

- **규칙 기반으로 되는 것만** 합니다 — 테스트 실행, 스택트레이스 기반 대상 추론, 메모리 검색.
- **저장하지 않습니다** — 요약을 만들 수 없으니 쓰레기를 저장하면 이후 검색 품질만 망칩니다.
- **LLM 을 다시 부르지 않습니다** — `ChatBedrock` 이 이미 4회 재시도한 뒤 던지는 예외입니다.
- 질의어는 `{언어} {파일명} {실패 출력}` 이라 저장 형식과 어긋나 **정확도가 낮습니다.**
  그래서 "참고용" 임을 화면과 리포트(`memory_degraded_query`)에 명시합니다.
- `install-hooks.sh` 의 `pre-push` 는 exit 5 를 **푸시 차단하지 않습니다** — 한도 소진은
  코드 결함이 아니므로 막으면 그날 아무도 푸시할 수 없게 됩니다.

### 비밀값 마스킹 — 막는 범위 (G10)

"모든 곳에서 비밀을 지운다" 고 과장하지 않는다. 어디까지 막고 어디는 막지 않는지 명시한다.

| 경로 | 마스킹 | 이유 |
|---|---|---|
| Bedrock 프롬프트 | ✗ 원본 유지 | 사용자 **자신의 AWS 계정**에서 처리되고 저장되지 않는다. 여기서 치환하면 파일 재생성이 마스크 문자열을 사용자 코드에 써 버린다 |
| `.heal/trace.log` | ✓ 마스킹 | 대상 저장소 안에 **파일로 남는다.** 커밋·공유될 수 있다 |
| LangSmith | ✓ 마스킹 | **제3자 SaaS 에 저장**되고 조직 구성원이 열람한다. `langsmith.Client(hide_inputs=...)` 로 업로드 시점에 가린다 |
| 콘솔 출력 | ✓ 마스킹 | 화면 캡처·CI 로그로 퍼진다 |

### 관측 (체크리스트 #11)

LCEL 체인으로 바꿔 둔 덕에 추적은 `Runnable` 단위로 자동 수집됩니다. `src/tracing.py` 가
하는 일은 "켤지 말지" 판단뿐입니다.

- **옵트인** — `LANGSMITH_TRACING` 과 `LANGSMITH_API_KEY` 가 **둘 다** 있어야 켜집니다.
  추적은 프롬프트를 외부로 보내고 그 안에는 사용자의 소스 코드 전문이 들어 있으므로,
  G6("승인 전까지 원본 불변")과 같은 정신으로 명시적 동의를 요구합니다.
- **키 없이는 켜지 않습니다** — 요청만 있고 키가 없으면 401 오류 덤프가 트레이스 ID 수십
  개와 함께 화면을 뒤덮어, 판단 근거를 보여 주는 화면 출력이 무력화됩니다.
- **무엇을 담는가** — 실행 하나가 트레이스 하나(`run_name`·언어 태그·테스트 명령·임계값),
  호출별로 시도 횟수와 메모리 주입 건수, 그리고 **G9 거부 사유(사라진 심볼)**. 마지막이
  가장 중요합니다 — 콘솔 한 줄로 흘려보내면 여러 실행에 걸친 패턴을 볼 수 없습니다.
- **`trace.log` 는 유지합니다** — 자격증명 없는 채점 환경에서도 근거가 남아야 합니다.
  `report.json` 의 `trace_enabled` 로 그 실행이 외부로 나갔는지도 기록됩니다.

### 가드레일 설계 원칙

**주석으로 약속하지 않고 구조로 강제합니다.**
예를 들어 G6 은 "승인 전에는 쓰지 마세요" 를 프롬프트에 쓰는 것이 아니라,
에이전트에게 애초에 원본 경로를 주지 않습니다. 노드들은 샌드박스 사본만 보고,
원본에 쓰는 코드는 `--apply` 분기 안 딱 한 줄뿐입니다.
또 G1 은 "테스트를 고치지 마세요"라고 프롬프트에 쓰는 것으로 끝내지 않습니다.
`is_source_file()` 이 테스트 파일을 애초에 수정 후보에서 제외하므로,
LLM 이 무엇을 출력하든 테스트 파일에는 도달할 수 없습니다.

---

## 채점 연결 (지우지 말 것)

| 채점 항목 | 이 문서에서 | 검증 방법 |
|---|---|---|
| SPEC 품질 | 1~5절 (필수 템플릿) + 부록 A·B (도메인·인아웃 상세) | — |
| 서비스 임팩트 | 1·2절 (재발 인식이라는 2차 가치·확장 관점) | — |
| 개발 완성도 | 3절 (노드 9개·어댑터 구조) | `tests/test_wiring.py` |
| 인-아웃 충족도 | 부록 B·C | `evaluation/run_inout.py`·`run_ragas.py` → `evaluation/round1_report.md`·`round2_report.md` |
| 가드레일 준수 | 4절 요약 + 부록 D 상세 (G1~G11) | `tests/test_guardrails.py` |
