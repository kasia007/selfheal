"""교육용 더미 데이터입니다. 일부러 버그를 심어 둔 파일입니다.

버그 계열: **부재 키 접근** — py-index 의 '경계 검사 누락' 과 같은 계열입니다.
같은 버그를 다시 돌리는 것은 캐시일 뿐 학습이 아니므로, 계열만 같고 형태는 다르게 두었습니다.
"""


def label_of(mapping, key):
    """매핑에서 key 의 값을 찾아 대문자로 돌려줍니다."""
    return mapping[key].upper()
