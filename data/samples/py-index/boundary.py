"""교육용 더미 데이터입니다. 일부러 버그를 심어 둔 파일입니다.

버그 계열: **경계 검사 누락** (범위를 벗어난 인덱스 접근)
1회차에서 이 패턴을 메모리에 학습시키는 용도입니다.
"""


def double_at(items, index):
    """리스트의 index 번째 값을 두 배로 돌려줍니다."""
    return items[index] * 2
